#include <array>
#include <ranges>
#include <experimental/meta>

template<class...> struct type_list { };

template<std::size_t N, class... Ts>
using take = typename [:substitute(^type_list, std::array{^Ts...} | std::views::take(N) | std::ranges::to<std::vector>()):];
